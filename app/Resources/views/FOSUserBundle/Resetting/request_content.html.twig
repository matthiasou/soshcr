{% trans_default_domain 'FOSUserBundle' %}

<div class="content-container">
	<div class="content-middle">
		<form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="col-lg-2 col-lg-offset-5 col-md-4 col-md-offset-4 fos_user_resetting_request">
		    <div class='form-group'>
		        <label for="username">Adresse e-mail</label>
		        <input class="form-control" type="text" id="username" name="username" required="required" />
		    </div>
		    <div class="clearfix"></div>
		    <div>
		        <input type="submit" class="btn btn-soshcr" value="{{ 'resetting.request.submit'|trans }}" />
		    </div>
		</form>
	</div>
</div>