{% extends 'SosBundle::layout.html.twig' %}

{% block sos_body %}
    <div id="poste_hotellerie" class="col-xs-12 sos-form">
        <h2 class="title-heading">Que veux tu faire {{user.prenom}}?</h2>
    </div>
    <br><br><br><br>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="box">
                    <div class="icon">
                        <div class="image"><i class="fa fa-refresh"></i></div>
                        <div class="info">
                            <h3 class="title">Mes actions</h3>
                            <p>
                                <ul >
                                    <li><a href="{{ path('validation') }}">Liste des candidats en attente de validation</li></a>
                                    <li><a href="{{ path('utilisateurs') }}">Liste des candidats</li></a>
                                    <li><a href="{{ path('recommandations') }}">Recommandations en attente de validation</li></a>
                                    <li><a href="{{ path('signalements') }}">Signalements en attente</li></a>
                                    <li><a href="{{ path('statistiques') }}">Statistiques</li></a>
                                </ul>
                            </p>
                            <div class="more">
                            </div>
                        </div>
                    </div>
                    <div class="space"></div>
                </div>
            </div>

{% endblock %}